extends ../layout

block example-content
  h1 Éxito! 🤓

  p
    | Tu transacción se ha realizado sin problemas. Estamos procesando tu orden. Gracias por confiar en nosotros 🙂.

  h2 Detalles de la transacción

  strong ID de la transacción:
  p= transaction.id
  strong Fecha de la transacción:
  p= new Date(transaction.created_at).toLocaleString()

  strong Monto:
  p $#{ transaction.payment.amount }
  strong Descripción:
  p= transaction.description
  strong Tipo de pago:
  p= transaction.payment.payment_type == 'credit' ? 'Crédito': 'Débito'
  strong Número de Cuotas:
  p= transaction.payment.installments

  a.btn.btn-primary(href="/")
    | Volver al Inicio

  a.btn.btn-primary(href="/examples/charge")
    | Volver al Ejemplo

  hr

  p
    | Respuesta de la API:
  pre
    code.language-javascript
      | !{ Prism.highlight(JSON.stringify(transaction, null, 2), Prism.languages.javascript) }